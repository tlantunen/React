const QuoteStyle = ({ name }) => {

    return (

        <div style={{ border: "1px solid gray" }}>
            
            <div>q: { name } <br></br></div>

        </div>
    );
};
 
export default QuoteStyle;   